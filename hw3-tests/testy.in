override void printOk(bool isOk) {
    if (isOk) print("ok");
}

override void printOk(bool mamamia, int x)
{
    return mamamia;
}

override void printOk(bool mamamia)
{
    return mamamia;
}

void main()
{
    int x = 4;
    byte y = 100 b;
    printOk(x>y);
}